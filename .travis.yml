sudo: false
language: ruby
rvm:
  - 2.3.1
cache:
  directories:
  - vendor/bundle
script:
  - bundle install
  - bundle exec rubocop
  - bundle exec rake install --trace
  - bundle exec rake test --trace
addons:
  apt:
    packages:
      - gcc-6-base
      - g++-6
      - cmake
      - libopencv-dev
    sources:
      - ubuntu-toolchain-r-test
      - kalakris-cmake